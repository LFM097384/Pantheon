# Pathfinder 2E 角色管理系统使用指南

## 功能概览

### 🏠 主页
- 快速导航到各个功能模块
- 根据用户角色（DM/玩家）显示不同的操作选项

### 👥 角色管理 (/character-manager)
完整的角色管理中心，提供以下功能：

#### 创建角色
1. 点击"创建角色"按钮
2. 填写角色名称
3. 选择族裔（人类、精灵、矮人、半身人、侏儒）
4. 选择职业（战士、法师、游荡者、牧师）
5. 系统会自动生成初始属性

#### 管理现有角色
- **编辑**：点击"编辑"按钮跳转到角色卡页面
- **复制**：快速复制现有角色作为模板
- **导出**：将角色数据导出为JSON文件
- **删除**：删除不需要的角色（需确认）

#### 导入导出功能
- **导出单个角色**：在角色列表中点击导出图标
- **导出所有角色**：点击页面顶部的"导出所有"按钮
- **导入角色**：点击"导入角色"按钮，支持拖拽上传JSON文件

### 📖 角色卡 (/character)
详细的角色属性编辑界面：

#### 基本信息
- 角色名称、等级、族裔、职业

#### 六大属性
- 力量、敏捷、体质、智力、感知、魅力
- 属性调整值自动计算：(属性值 - 10) ÷ 2（向下取整）

#### 战斗数据
- **护甲等级(AC)**：10 + 敏捷调整值
- **当前生命值**：可手动调整，不能超过最大值
- **最大生命值**：8 + 体质调整值 + (等级-1) × (6 + 体质调整值)

#### 实时计算
- 所有衍生数值都会根据基础属性实时更新
- 修改任何基础属性都会立即反映在相关计算中

## 数据存储

### 本地存储格式
- 每个角色以 `pf_character_{角色ID}` 为键存储在localStorage中
- 数据格式为JSON，包含完整的角色信息

### 导入导出格式
```json
{
  "formatVersion": "1.0",
  "exportedAt": "2025-07-22T06:35:00.000Z",
  "character": {
    "id": "char_1674123456789",
    "name": "角色名称",
    "level": 1,
    "ancestry": "human",
    "class": "fighter",
    "ruleSystem": "pathfinder",
    "stats": {
      "strength": 16,
      "dexterity": 14,
      "constitution": 15,
      "intelligence": 10,
      "wisdom": 12,
      "charisma": 8,
      "hitPoints": 10,
      "maxHitPoints": 10,
      "armorClass": 12
    },
    "createdAt": "2025-07-22T06:35:00.000Z",
    "updatedAt": "2025-07-22T06:35:00.000Z"
  }
}
```

## 使用流程

### 新用户创建角色
1. 访问角色管理页面 (/character-manager)
2. 点击"创建角色"
3. 填写基本信息并保存
4. 点击"编辑"进入角色卡页面
5. 调整属性值到满意的配置
6. 点击"保存"保存更改

### 导入现有角色
1. 在角色管理页面点击"导入角色"
2. 选择或拖拽JSON格式的角色文件
3. 系统会自动导入并显示在角色列表中

### 备份角色数据
1. 定期使用"导出所有"功能备份角色数据
2. 将导出的JSON文件保存在安全位置
3. 需要时可以通过导入功能恢复数据

## 技术特性

- ✅ 实时计算和显示
- ✅ 本地数据持久化
- ✅ 完整的导入导出功能
- ✅ 响应式界面设计
- ✅ 数据验证和错误处理
- ✅ 符合Pathfinder 2E规则

## 未来扩展

系统设计支持未来添加：
- 技能系统
- 法术管理
- 装备系统
- 专长和特性
- 多规则系统支持
